{
    "name": "school-grades-frontend",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && node scripts/inject-hash.js",
        "preview": "vite preview",
        "test": "node --test tests/frontend/*.test.js"
    },
    "devDependencies": {
        "jsdom": "^24",
        "vite": "6.0.0",
        "vitest": "4.1.0"
    }
}
